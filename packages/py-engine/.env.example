# Negotiation Dojo LLM (Google Gemini). The driver reads GEMINI_API_KEY first,
# then GOOGLE_API_KEY. Loaded by decision_forge.main at sidecar startup.
# The desktop app normally supplies this via the OS keychain; this file is for
# running the sidecar / smoke test standalone.
GOOGLE_API_KEY=
